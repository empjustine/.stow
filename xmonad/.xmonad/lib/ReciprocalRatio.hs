-- ~/.xmonad/lib/ReciprocalRatio.hs
-- =================================

module ReciprocalRatio where

goldenRatio = (2/(1+(toRational(sqrt(5)::Double))))
silverRatio = (1/(1+(toRational(sqrt(2)::Double))))
iso216Ratio = (1/(toRational(sqrt(2)::Double)))
